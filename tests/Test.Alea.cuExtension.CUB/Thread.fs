module Test.Alea.cuExtension.CUB.Thread

    module Load =
        let f() = "load"

    module Operators =
        let f() = "operators"

    module Reduce =
        let f() = "reduce"

    module Scan =
        let f() = "scan"

    module Store =
        let f() = "store"
